import styled from 'styled-components';
import colors from '../index';

export default styled.span`
  color: ${colors.grey};
  position: absolute;
  top: 70px;
  left: 20px;
  display: none;
  white-space: nowrap;


`;